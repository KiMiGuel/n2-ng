#!/usr/bin/env bash
# N2-ng launcher
# Runs the Python GUI, which will prompt for sudo if not root.
cd "$(dirname "$0")" || exit 1
export PYTHONPATH="$PWD/src${PYTHONPATH:+:$PYTHONPATH}"
exec python3 -m n2ng.main "$@"
